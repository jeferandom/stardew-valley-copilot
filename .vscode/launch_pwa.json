{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Launch Expo in Chrome",
            "url": "http://localhost:8081",
            "webRoot": "${workspaceFolder}"
        }
    ]
}